name: CI Pipeline

on:
  workflow_dispatch:
  # pull_request:
  #   types: [opened, synchronize]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run fake test
        run: echo "Running tests..."
